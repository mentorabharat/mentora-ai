# Canonical Boundaries

- JSON defines structure, not UI
- JSON does not store videos or media
- JSON cannot be edited by AI
- AI can read JSON but cannot modify it
- All JSON changes require human approval
- Versioning is mandatory for safety
